Short backend code assignment downloading https://books.toscrape.com


Depends on:

 * pip install asyncio
 * pip install aiohttp
 * pip install aiofiles
 * pip install beautifulsoup4


TODO:

 * Error handling, i.e. exception handling in the tasks
 * Parameters for web URL and download location
